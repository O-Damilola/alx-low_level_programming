#ifndef HEADER_H
#define HEADER_H

#include <stdio.h>
#include <stdlib.h>

int add(int x, int y);
int sub(int x, int y);
int mul(int x, int y);
int division(int x, int y );
int mod(int x, int y);

#endif
